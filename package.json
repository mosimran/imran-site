{
  "name": "imran-site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.3.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/csp-hashes.mjs",
    "preview": "astro preview",
    "check": "astro check && node scripts/check-budget.mjs && node scripts/check-links.mjs && node scripts/check-errata.mjs && node scripts/check-expiry.mjs",
    "budget": "node scripts/check-budget.mjs",
    "links": "node scripts/check-links.mjs",
    "placeholders": "node scripts/placeholders.mjs",
    "social": "node scripts/make-social.mjs",
    "errata": "node scripts/check-errata.mjs",
    "expiry": "node scripts/check-expiry.mjs"
  },
  "dependencies": {
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.0",
    "@astrojs/sitemap": "^3.7.3",
    "typescript": "^5.6.0"
  }
}
