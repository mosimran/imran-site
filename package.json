{
  "name": "imran-site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.3.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check && node scripts/check-budget.mjs && node scripts/check-links.mjs",
    "budget": "node scripts/check-budget.mjs",
    "links": "node scripts/check-links.mjs",
    "placeholders": "node scripts/placeholders.mjs",
    "social": "node scripts/make-social.mjs"
  },
  "dependencies": {
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.0",
    "@astrojs/sitemap": "^3.7.3",
    "typescript": "^5.6.0"
  }
}
